Review the generated React code.
1. Add `react-helmet-async` for managing Head tags (Title/Meta).
2. Create a `SeoHead.tsx` component that accepts title, description, and city.
3. Implement this component on every page.
4. Ensure all images have `alt` text.
5. Ensure the Hero Video has the correct `poster` and `aria-label`.