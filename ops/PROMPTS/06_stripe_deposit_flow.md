Write a Node.js Express route function that interacts with the Stripe API.
Function: `createDepositSession`.
Input: `customerEmail`, `amount` (default 5000 cents).
Output: Stripe Session URL.
Ensure error handling is robust.